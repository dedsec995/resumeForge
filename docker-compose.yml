services:
  resume-forge-backend:
    build: .
    container_name: resume-forge-backend
    ports:
      - "9241:9241"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./firebase.json:/app/firebase.json:ro
    user: "1001:1001"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9241/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - resume-forge-network

networks:
  resume-forge-network:
    driver: bridge 